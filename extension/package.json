{
    "name": "quick-dl-helper",
    "version": "0.4",
    "main": "background.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "lint": "npx web-ext lint",
        "build": "npx web-ext build --overwrite-dest",
        "sign": "npx web-ext sign --channel=unlisted --amo-metadata=metadata.json --api-key=$AMO_JWT_ISSUER --api-secret=$AMO_JWT_SECRET"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "devDependencies": {
        "web-ext": "^8.3.0"
    }
}
